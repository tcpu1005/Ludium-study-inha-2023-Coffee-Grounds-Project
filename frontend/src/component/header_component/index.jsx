import React from 'react';
import { HeaderStyle } from '../../page/collection_page/style';

const Header = () => {
    return (
        <HeaderStyle>
            <button>←</button>
            <button>☰</button>
        </HeaderStyle>
    );
};

export default Header;
